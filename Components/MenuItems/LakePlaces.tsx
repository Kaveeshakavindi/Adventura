import React from 'react';
import { Text} from 'react-native';

interface props {}

const LakePlaces: React.FC<props> = (): JSX.Element => (
    <Text>
        Lake
    </Text>
);
export default LakePlaces;
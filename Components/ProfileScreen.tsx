import React from 'react';
import { Text } from 'react-native';

interface props {}

const ProfileScreen: React.FC<props> = (): JSX.Element => (
    <Text>
        Profile Screen
    </Text>
);
export default ProfileScreen;